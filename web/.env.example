# Public — safe to expose to the browser
NEXT_PUBLIC_SITE_URL=https://www.rif.co.id
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_GTM_ID=

# Server-only — never prefix these with NEXT_PUBLIC_.
# Filled in during the Strapi integration (Phase 3); see
# "docs/API - RIF Corporate Website.md".
STRAPI_URL=
STRAPI_API_TOKEN=
